<resources>
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
        <item name="android:windowBackground">@color/bootsplash_background</item>
    </style>

    <style name="BootTheme" parent="Theme.BootSplash">
        <item name="windowSplashScreenBackground">@color/bootsplash_background</item>
        
        <!-- ⚡ CHANGE THIS LINE: Remove transparent, match the background color -->
        <item name="windowSplashScreenIconBackgroundColor">@color/bootsplash_background</item>

        <item name="bootSplashBackground">@color/bootsplash_background</item>
        <item name="bootSplashLogo">@drawable/bootsplash_logo</item>
        <item name="postBootSplashTheme">@style/AppTheme</item>
    </style>
</resources>